import styled from 'styled-components'

export const PriseContainer = styled.p`
    display: flex;
    margin: -10px 0;
    font-size: 20px;
    align-items: flex-start;

    span{
        font-weight: 600;
    }
    small{
        font-size: small;
        margin-top: -2px;
    }
`